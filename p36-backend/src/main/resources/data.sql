/*DROP TABLE IF EXISTS users;
CREATE TABLE users (
  id int(11) primary key,
  firstName varchar(255),
  lastName varchar(255),
  username varchar(255),
  emailAddress varchar(255),
  password varchar(255),
  role varchar(50)
);

INSERT INTO users(id, firstName, lastName, username, emailAddress, password, role)
 VALUES (1, 'Donald', 'Duck', 'dd', 'dd@duck.com', 'duck', 'ROLE_USER');*/
